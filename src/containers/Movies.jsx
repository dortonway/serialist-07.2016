import React, { PropTypes } from 'react';
import Header from '../components/Header/Header.jsx';

export default class Movies extends React.Component {

  render() {
    return (
      <div>
        <Header activeType="movies" />
        Movies page hasn't been developed yet
      </div>
    );
  }

}